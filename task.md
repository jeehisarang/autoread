작업지시서 — 정지 시 UI 멈춤 수정 + 보드 장수 디바운스
날짜: 2026-08-21
우선순위: 높음
==================================================

목표


기록 정지 버튼을 눌러도 프로그램이 멈추지 않게 한다.
보드 장수(3→4→5)가 한 번의 오측정으로 올라가지 않게 한다.


수정 범위


정지 시 스레드 종료 방식
보드 장수 상승 판정 로직
핵심 파싱/닉네임 매칭/행동 기록 로직은 건드리지 말 것


요구사항

[정지 시 멈춤]

thread.join()을 메인 스레드에서 직접 호출하지 않는다
백그라운드에서 종료 처리하고, 완료되면 UI를 안전하게 갱신한다
정지 버튼을 눌러도 화면이 “응답 없음” 상태가 되면 안 된다

[보드 장수]

로컬 보드 장수가 증가할 때도 연속 확인(디바운스)을 적용한다
한 번의 측정으로 바로 3→4, 4→5로 올리지 않는다
플랍/턴/리버 정상 진행에는 과도한 지연이 없어야 한다


성공 기준


정지 버튼 클릭 시 UI가 멈추지 않음
보드 장수가 조기에 잘못 올라가지 않음
기존 행동 기록/순서 유지 기능에 회귀 없음