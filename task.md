작업지시서 - 히어로 닉네임 설정값 매칭

날짜: 2026-08-20
우선순위: 상
범위: 히어로 식별만.
스트리트/폴드중단/동기화/딜러채팅 파서 핵심은 건드리지 말 것.

--------------------------------------------------
0. 목표

히어로를 "하단 중앙 OCR로 매번 찾기" 대신
사용자가 미리 넣어둔 닉네임으로 식별한다.

관전/실전, 6인/7인 상관없이
딜러 채팅에 그 이름이 보이면 "나"로 표시.

--------------------------------------------------
1. 설정 방식

1) UI에 히어로 닉네임 입력란 추가
   - 예: "히어로 닉네임"
   - 기록 시작 전에 입력 가능
   - 비어 있으면 기존 방식(하단 OCR) 폴백 가능하면 유지,
     어렵면 이번엔 설정값 필수여도 됨

2) 입력한 값은 그 세션 동안 유지
   - 매 폴링마다 좌석 OCR로 닉네임을 다시 찾지 않음

3) 코드에 특정 닉네임을 하드코딩하지 말 것

--------------------------------------------------
2. 매칭 규칙

딜러 채팅에서 읽힌 이름과 설정 닉네임 비교:

- 완전 일치
- 또는 접두어 일치 (말줄임표 대비)
- 또는 편집거리 1 (기존 히어로 매칭 허용 수준)

매칭되면:
  나 (포지션) : 행동
형식으로 출력 (기존 포지션 표시 유지)

매칭 실패 시:
  일반 플레이어처럼 출력 (이름 접두어/? 규칙 유지)

--------------------------------------------------
3. 홀카드 / 레이아웃

이번 범위에서 필수 아님.

다만:
- 홀카드 AI는 가능하면 기존대로 유지
- 6max/7max 좌표 자동 전환은 다음 단계로 미룸
- 이번 작업으로 홀카드가 갑자기 깨지면 보고

--------------------------------------------------
4. 하지 말 것

- 인원수 컴퓨터비전 자동 감지 대공사
- table_layout 강제 교체만으로 끝내기
- 스트리트/동기화/폴드중단 로직 변경
- 닉네임 하드코딩

--------------------------------------------------
5. 성공 기준

1. 설정에 닉네임을 넣으면
   딜러 채팅의 해당 플레이어 행동이
   "나 (포지션) :" 로 나온다

2. 닉네임을 바꾸면 다른 계정으로도 동작

3. 관전/실전 모두, 설정값만 맞으면 매칭됨

4. 기존 기능 유지
   - 스트리트 헤더
   - 폴드 후 중단
   - 이름 접두어
   - 시작 동기화

--------------------------------------------------
6. 테스트

- 설정에 실전 닉네임 입력 후 관전 또는 실전 1~2분
- 로그에 "나 (BB) :" 형태 확인
- 닉네임 비우거나 틀린 값 넣었을 때
  나 로 잘못 안 붙는지 확인

--------------------------------------------------
7. 보고

- 수정 한 줄 요약
- 설정 UI/저장 위치
- 매칭 성공 예시
- 부작용 여부